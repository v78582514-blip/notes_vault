name: notes_vault
description: A simple smart notes app
publish_to: 'none' # не публикуем

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # вот это добавь ↓↓↓
  shared_preferences: ^2.2.3

  # остальные стандартные зависимости, если они есть
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
